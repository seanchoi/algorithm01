// push front
s = [1,2,3,4,5]
function push_front(arr, value){
    new_arr=[]
    new_arr.push(value)
    for(i=0; i<length(arr); i++);
        new_arr.push(arr[i])
    print(new_arr)
    return new_arr
};

push_front(s, 9)
